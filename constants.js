
//Constants used in other files
const IP = 'localhost'; // IP address 
const PORT = 50541; //PORT number 

const name = 'JEP'//User initials

//Contants being exported as properties inside of an object
module.exports = {
  IP,
  PORT,
  name,
};